{% extends 'Base2.html.twig' %}

{% block content %}
    <div class="inner-content loginWrp">
        <div class="container">


            <div class="row">
                <div class="col-md-9 col-sm-8">
                    <div class="heading-title">Showing <span>All Resumes</span></div>
               <ul>
                   {% for m in members %}
                   <li>
                       <div class="listWrpService featured-wrap candidate">
                           <div class="row">
                               <div class="col-md-2 col-sm-3 col-xs-12">
                                   <div class="listImg"><img src="{{asset("uploads/images/"~m.photo)}}" alt=""></div>
                               </div>
                               <div class="col-md-10 col-sm-9 col-xs-12">
                                   <div class="row">
                                       <div class="col-md-8">
                                           <h3><a href="#">{{ m.nom }}</a></h3>
                                           <div class="designation">{{ m.rotaract }}</div>
                                           <ul class="featureInfo">
                                               <li><i class="fa fa-map-marker" aria-hidden="true"></i> {{ m.adresse }}</li>

                                           </ul>

                                       </div>
                                       <div class="col-md-4">
                                           <ul>
                                               <li class="click-btn apply"><a href="{{ path('member_show',{'id':m.id}) }}"><i class="fa fa-file-text-o" aria-hidden="true"></i> Voir plus</a></li>
                                           </ul>
                                       </div>
                                   </div>
                               </div>
                           </div>
                       </div>
                   </li>
                   {% endfor %}
               </ul>
                </div>
            </div>
            </div>
        </div>
    </div>
{% endblock %}
